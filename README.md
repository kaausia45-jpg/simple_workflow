# Simple Workflow Bot

This script automates the process of analyzing an Excel file.

## Setup
1. Install required packages: `pip install -r requirements.txt`
2. Place your data file as `input_data.xlsx` in this directory.
3. Configure your email settings in `main.py`.

## Run
`python main.py`
